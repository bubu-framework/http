# List of Exception code

The code are here for if the message had to change, the code don't change.

## SessionException

`Undefined key` => 404

## HttpRequireException

`HTTPS is required` => 403.4
